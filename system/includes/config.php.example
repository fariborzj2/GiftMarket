<?php
// Database Configuration
define('DB_HOST', 'localhost');
define('DB_NAME', '');
define('DB_USER', '');
define('DB_PASS', '');

// System Configuration
define('SITE_NAME', 'UAE.GIFT');
// BASE_URL should include protocol and trailing slash (e.g., http://yourdomain.com/)
// Comment the line below for automatic detection
define('BASE_URL', 'http://localhost/');
define('INSTALLED', false);

// Error Reporting
error_reporting(E_ALL);
ini_set('display_errors', 1);

// Start Session
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
